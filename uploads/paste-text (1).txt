You are enhancing an existing Atoms‑built application that already has an admin user account in the system. The admin can already log in and has full privileges; do not redefine core auth or the initial admin creation.

1. Add admin account management functionality:
   - Create an API endpoint to edit the existing admin’s email:
     - Only allow the authenticated admin user to access this endpoint.
     - Accept a new email address in the request body.
     - Validate the email format and ensure it does not conflict with any other user in the system.
     - Update the admin’s email in the database and return a success response (e.g., { success: true }).
   - Create a separate API endpoint to edit the admin’s password:
     - Only accessible to the authenticated admin.
     - Accept a new password (and optionally an “oldPassword” if your design requires it).
     - Hash the new password with a strong algorithm (e.g., bcrypt/scrypt).
     - Update the hashed password in the database and confirm success.

2. Implement a default feedback email setting:
   - Create a small “Settings” or “AppConfig” entity/table that stores application‑wide configuration, including:
     - A string field `defaultFeedbackEmail` (nullable).
   - Add two endpoints:
     - GET /api/settings/feedback-email → returns { defaultFeedbackEmail: string | null }.
     - PUT /api/settings/feedback-email → accepts { defaultFeedbackEmail: string } and validates it as an email; saves or updates the default feedback email.
   - Ensure these endpoints are also protected so that only an authenticated admin can read or change `defaultFeedbackEmail`.

3. Input and validation:
   - Use strict schema validation (e.g., Zod, Joi, or plain middleware) for:
     - Admin email (must be a valid email, not empty).
     - Password (minimum length; optional on update unless security requires current‑password check).
     - defaultFeedbackEmail (must be a valid email or null).
   - Return clear, non‑verbose error messages without leaking stack details.

4. Security and access:
   - Reuse the existing admin‑authentication and admin‑authorization logic in the Atoms‑built app.
   - Confirm that:
     - Only the admin can access the email/password‑edit and feedback‑email‑settings endpoints.
     - No password hashes or secrets appear in responses or logs.

5. Code structure:
   - Extend the existing codebase by creating/adding:
     - routes/admin.js (or extend existing admin route file).
     - controllers/adminController.js with methods for updateEmail and updatePassword.
     - services/adminService.js (if not already present) to handle password hashing and DB operations.
     - Optionally routes/settings.js and controllers/settingsController.js for the default feedback‑email feature.
   - Use async/await and proper error handling (try/catch or middleware).

6. API documentation (optional):
   - Add OpenAPI‑style comments or annotations for:
     - PUT /api/admin/email
     - PUT /api/admin/password
     - PUT /api/settings/feedback-email
     - GET /api/settings/feedback-email
   - Include example request bodies and responses.

Implement the above in a clean, non‑disruptive way that fits the existing Atoms‑built app structure. Do not regenerate core auth or the initial admin setup; assume those are already in place.