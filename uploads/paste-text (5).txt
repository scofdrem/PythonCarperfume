In my existing e‑commerce application built with Atoms‑dev, add a feedback inquiry form triggered by an “Уточнить наличие” link/button placed prominently on the product card under the accordion.
The form opens as a responsive modal overlay with a clean, modern design matching the app’s theme (use existing color scheme, fonts, and shadows). Include:
Channel selector:
Dropdown titled “Способ связи”. Options: Telegram, Email, Phone, Viber.
Dynamically show an input field below:
Telegram: Username input (e.g., @username).
Email: Email input (required) with format validation (valid email regex).
Validate all fields as required and apply format‑specific rules.
Products multi‑select:
Searchable multi‑select dropdown/combobox pulling from the existing product catalogue.
Allow selecting one or multiple products (show: product name, image thumbnail, price).
Pre‑populate with:
The current product if the user is on the product detail page, or
Cart items if triggered from the cart.
Use live search/filter as the user types.
Message textarea:
Free‑text box with a 1000 character limit.
Show a counter (e.g., 0/1000) and a placeholder:
"Additional details about availability inquiry...".
Footer actions:
“Отправить” button (primary, submits the form).
“Отменить” button (secondary, closes the modal without saving).
On Send:
Validate all fields.
Show a loading spinner during submission.
Send the form data to a designated recipient email address (e.g., support@yourstore.com) using the app’s backend (existing mailing service or API such as nodemailer, SMTP, or a custom email endpoint).
Payload to include: selected channel, user contact (Telegram username / email / phone / Viber number), selected products, message text, current timestamp, and user‑context info if available (e.g., user ID or session ID).
If there’s no existing email integration yet, create a simple backend endpoint that:
Stores the inquiry in the app’s database (with fields: channel, user contact, products, message, status, timestamp).
Also sends an email to the fixed feedback address (e.g., feedback@yourapp.com) with a formatted HTML/plain‑text summary of the inquiry.
Success:
Show a toast notification: "Мы скоро с вами свяжемся!", then close the modal.
Error:
Show inline field‑level errors (e.g., invalid email, empty required fields) without page reload.
Ensure the form is:
Mobile‑friendly (touch‑optimized, responsive layout).
Accessible: proper ARIA labels, focus management, and keyboard navigation.
Integrated without page reload (use client‑side routing / SPA style).
Styled to blend perfectly with the current app UI (reuse existing button styles, typography, shadows, and spacing).