**Implement bulk edit functionality for product list in the admin panel as follows:**

- **Add select checkboxes:** In the product list table/grid, add a checkbox column at the start of each row (leftmost). Include a "Select All" master checkbox in the table header. Checkboxes should support multi-select with Ctrl/Shift modifiers for range selection.

- **Bulk change Бренд (Brand):** Add a bulk actions dropdown or buttons above the product list:
  - "Bulk Edit Brand" button → opens modal with:
    - Current brand display (or "Mixed")
    - New brand dropdown/search input (populate from existing brands)
    - "Apply" and "Cancel" buttons
  - On Apply: Confirm changes with modal ("Change brand for X selected products? Yes/No").

- **Bulk change Категория (Category):** Similar to Brand:
  - "Bulk Edit Category" button → modal with new category selector (dropdown/tree from existing categories).
  - Confirmation modal before applying.

- **Bulk change Тег (Tag):** Similar:
  - "Bulk Edit Tags" button → modal with:
    - Multi-select tag chips or input (add/remove tags).
    - Existing tags list for selected products.
  - Confirmation modal ("Update tags for X products? Yes/No").

- **General requirements:**
  - Only enable bulk buttons when ≥1 products selected; show count (e.g., "3 selected").
  - On confirm Yes: Update selected products' data (array/object properties: brand, category, tags), refresh list, show success toast ("Updated X products").
  - On Decline/No: Close modal, no changes.
  - Handle edge cases: No selection, mixed values, validation (required fields).
  - Use existing data structures/UI components (match admin theme).
  - Client-side only, persist changes to localStorage or mock DB.
  - Responsive, accessible (keyboard nav, ARIA).
  - Test: Select multiple, bulk change, confirm/decline, verify updates.

**Output only the complete, working code changes (diffs, full components/files). Production-ready, bug-free.** [0xminds](https://0xminds.com/blog/guides/build-admin-panel-ai-prompts-tutorial)